<h1>The word : <%=@entry.word%></h1>
<h3>origin language: <%=@entry.language%> </h3>
<%= @entry.definition %>
<p><%= link_to("Back to word list", entries_url)%>
</p>
<%= link_to("Edit", edit_entry_url(@entry))%> | <%=link_to("Delete",entry_url(@entry), method: :delete, data: {confirm: "Are you sure you want to delete the Word '#{@entry.word}'? This cannot be undone!"} ) %>
